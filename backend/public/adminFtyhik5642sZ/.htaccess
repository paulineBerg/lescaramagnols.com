RewriteEngine On

# Autorise l'accès aux fichiers et dossiers existants
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Sinon, route toutes les autres requêtes vers index.php
RewriteRule ^ index.php [QSA,L]
