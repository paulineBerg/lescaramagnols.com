# Audit sécurité - Phase 5 - 2026-07-19

Exécution initiale : Claude Code, sur autorisation utilisateur (tâche `.ai/CURRENT_TASK.md`, niveau C).
Vérification indépendante : Codex, 2026-07-19.
Périmètre : Lot 1 local/HTTP, correction `npm audit fix`, puis Lot 2 SSH OVH lecture seule.
Aucune ligne brute de log, IP, identifiant, secret ou sortie `.env` détaillée n'est archivée ici.

## Synthèse

| Item checklist phase 5 | Statut | Résultat |
|---|---|---|
| `composer audit` (backend) | Exécuté | **0 advisory** |
| `npm audit` (frontend) | Exécuté puis corrigé | 11 vulnérabilités initiales, toutes en devDependencies ; **0 vulnérabilité après `npm audit fix`** |
| `check-security-headers` sur la prod | Exécuté (HTTP GET local) | **OK** - tous les headers requis présents, statut 200 |
| Expiration certificat | Exécuté (handshake TLS local) | Let's Encrypt, valide jusqu'au **2026-10-02** (~75 jours) |
| `check-env --strict-prod-security` sur la prod | Exécuté via SSH lecture seule | **OK** |
| Revue `security.log` / `check-log-alerts` prod | Exécuté via SSH lecture seule | Alerte globale `error` : 10 rejets CSRF privés et 2 échecs d'envoi d'invitation privée sur 7 jours |
| Rotation/restaurabilité sauvegardes | Exécuté via SSH lecture seule | Dry-run OK, 10 jeux récents observés, dernier jeu vérifié par structure archive + SHA-256 |
| Challenge ACME | Non vérifiable depuis le dépôt | Géré côté OVH ; seul le certificat servi est observable |

## Détail

### 1. Audit dépendances backend (`composer audit`)

Commande : `composer audit --no-interaction` (backend, 2026-07-19).
Résultat : `No security vulnerability advisories found.` - aucune vulnérabilité connue.

### 2. Audit dépendances frontend (`npm audit`)

Commande : `npm audit` (frontend, 2026-07-19).
Résultat : **11 vulnérabilités** (1 low, 3 moderate, 5 high, 2 critical).

**Classification : exposition production nulle.** Le `package.json` frontend ne déclare
aucune dépendance runtime (`dependencies` absent) ; tous les paquets touchés sont des
`devDependencies` (chaîne de build/test Vite). Le site publié est un build statique :
ces vulnérabilités n'affectent que le poste de développement (serveur de dev Vite,
runner de tests), pas la production.

| Paquet | Versions touchées | Sévérité | Advisory | Nature |
|---|---|---|---|---|
| vitest | < 3.2.6 | **critical** | GHSA-5xrq-8626-4rwp | Lecture/exécution de fichier arbitraire si le serveur UI Vitest écoute (dev only) |
| vite | 7.0.0 – 7.3.3 | high | GHSA-4w7w-66w2-5vf9, GHSA-v2wj-q39q-566r, GHSA-p9ff-h696-f583, GHSA-v6wh-96g9-6wx3, GHSA-fx2h-pf6j-xcff | Path traversal / lecture de fichiers via serveur de dev (dev only) |
| ws | 8.0.0 – 8.20.1 | high | GHSA-58qx-3vcg-4xpx, GHSA-96hv-2xvq-fx4p | Divulgation mémoire / DoS (dépendance transitive outillage) |
| esbuild | 0.27.3 – 0.28.0 | moderate | GHSA-g7r4-m6w7-qqqr | Lecture arbitraire de fichier via serveur de dev sur Windows (dev only) |
| fast-uri | <= 3.1.1 | high | GHSA-q3j6-qgpj-74h6, GHSA-v39h-62p7-jpjc | Traversée de chemin / confusion d'hôte (dépendance transitive outillage) |
| form-data | 4.0.0 – 4.0.5 | high | GHSA-hmw2-7cc7-3qxx | Injection CRLF dans noms de champs/fichiers multipart (dépendance transitive outillage) |
| brace-expansion | < 1.1.13 ou >= 2.0.0 < 2.0.3 | moderate | GHSA-f886-m6hf-6m8v | Déni de service par séquence invalide (dépendance transitive outillage) |
| js-yaml | 4.0.0 – 4.1.1 | moderate | GHSA-h67p-54hq-rp68 | Déni de service par alias répétés (dépendance transitive outillage) |
| postcss | < 8.5.10 | moderate | GHSA-qx2v-qp2m-jg93 | XSS via `</style>` non échappé en sortie stringify (build) |
| picomatch, @vitest/coverage-v8 | — | low/moderate | transitives | Dépendances des paquets ci-dessus |

Correction appliquée ensuite par Codex : `npm --prefix frontend audit fix`.
Résultat : 28 paquets mis à jour dans `frontend/package-lock.json`, `package.json`
inchangé, `npm --prefix frontend audit --audit-level=low` retourne **0 vulnérabilité**.
Validations après correction : frontend test, lint, build, hygiene docs et `git diff --check` OK.

### 3. Headers de sécurité production

Commande : `composer check-security-headers -- --url=https://www.lescaramagnols.com` (GET HTTP depuis le poste local).
Résultat : statut final 200, **tous les headers requis présents** (CSP, X-Frame-Options,
X-Content-Type-Options, Referrer-Policy, Permissions-Policy, COOP, CORP).

### 4. Certificat TLS production

Commande : handshake TLS local (`openssl s_client` + `x509 -noout -dates`).
Résultat : certificat `CN=www.lescaramagnols.com`, émetteur Let's Encrypt (YE1),
valide du 2026-07-04 au **2026-10-02**. ~75 jours de marge au 2026-07-19 ; le
renouvellement récent (04/07) indique que le renouvellement automatique fonctionne.
Le bon fonctionnement du challenge ACME lui-même n'est observable que côté OVH.

### 5. Configuration production (`check-env`)

Commande : `php core/tools/check_env.php --strict-prod-security` exécutée sur le backend OVH via SSH lecture seule.
Résultat : **OK** pour l'environnement `production`.

### 6. Revue logs production (`check-log-alerts`)

Commande : `php core/tools/check_log_alerts.php --since-minutes=10080 --webhook-url= --email-to= --json`
exécutée sur OVH. Les notifications ont été désactivées explicitement pour le diagnostic.

Résultat agrégé sur 7 jours :

| Signal | Compte | Seuil | Sévérité |
|---|---:|---:|---|
| `private.csrf_rejected` | 10 | 3 | warning |
| `private.email_failed` | 2 | 1 | error |

Détail anonymisé :
- les rejets CSRF privés sont concentrés le 2026-07-18 ;
- les échecs email correspondent à 2 échecs d'envoi d'invitation privée le 2026-07-17 ;
- aucun signal agrégé sur rate-limit, backup failed/warning, purge failed, HTTP 403/429 ou cron failed.

À suivre dans une tâche dédiée : investiguer les échecs d'invitation privée et vérifier si les rejets CSRF correspondent à des expirations de session attendues ou à un problème d'ergonomie/session.

### 7. Sauvegardes production

Commande : `php core/tools/backup_production.php --dry-run --json` exécutée sur OVH.
Résultat : dry-run **OK**, configuration sauvegarde complète, racine des sauvegardes hors racine backend.

Inspection lecture seule :
- 10 archives fichiers, 10 dumps SQL et 10 manifestes observés ;
- dernier jeu daté du 2026-07-19 à 01:00 ;
- dernier manifeste lisible, scope `all`, non dry-run ;
- archive fichiers valide (`tar -tzf`) ;
- dump SQL gzip valide (`gzip -t`) ;
- empreintes SHA-256 fichiers et SQL conformes au manifeste.

Limite : la restaurabilité réelle complète exige un test de restauration selon runbook. Ce test n'a pas été exécuté dans ce lot lecture seule.
